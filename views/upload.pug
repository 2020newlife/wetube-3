extends layouts/main

block content
    .form-container
        form(name="uploadForm", action=urls.upload, method="post", enctype="multipart/form-data")
            .upload-file
                label(for="file") Video File
                input(type="file", id="file" name="videoFile", placeholder="Video File", required=true, accept="video/*")
            input(type="text", name="title", placeholder="Title", required=true)
            input(type="textarea", name="description", placeholder="description")
            input(type="submit", value="Upload Video")