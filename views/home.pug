extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
        each video in videos 
            +videoBlock(video)
            
            